#
# Be sure to run `pod lib lint morpheus.podspec' to ensure this is a
# valid spec before submitting.
#
# Any lines starting with a # are optional, but their use is encouraged
# To learn more about a Podspec see https://guides.cocoapods.org/syntax/podspec.html
#

Pod::Spec.new do |s|
  s.name             = 'MPopup'
  s.version          = '2.14.37'
  s.summary          = 'A short description of MPopup.'

# This description is used to generate tags and improve search results.
#   * Think: What does it do? Why did you write it? What is the focus?
#   * Try to keep it short, snappy and to the point.
#   * Write the description between the DESC delimiters below.
#   * Finally, don't worry about the indent, CocoaPods strips it!

  s.description      = <<-DESC
TODO: Add long description of the pod here.
                       DESC

  s.homepage         = 'https://gitlab.morpheus.kr/MPopup'
  # s.license          = { :type => 'MIT', :file => 'LICENSE' }
  s.author           = { 'danteryung' => 'dantexx@uracle.co.kr' }
  s.source           = { :git => 'https://gitlab.morpheus.kr', :tag => s.version.to_s }
  # s.social_media_url = 'https://twitter.com/<TWITTER_USERNAME>'

  s.ios.deployment_target = '9.0'

  # s.source_files = 'morpheus/Classes/**/*'
  
  # s.resource_bundles = {
  #   'morpheus' => ['morpheus/Assets/*.png']
  # }
  s.resources = "MPopup.bundle"
  s.ios.vendored_frameworks = "MPopup.xcframework"
  s.static_framework = true
  # s.vendored_libraries = "MCore.license"
  s.frameworks = 'UIKit', 'Foundation'
  # s.libraries = 'xml2', 'z'
  # s.pod_target_xcconfig = {
  #   'EXCLUDED_ARCHS[sdk=iphonesimulator*]' => 'arm64'
  # }
  # s.user_target_xcconfig = { 'EXCLUDED_ARCHS[sdk=iphonesimulator*]' => 'arm64' }
  # s.frameworks = 'UIKit', 'MapKit'
  # s.dependency 'AFNetworking', '~> 2.3'
end
