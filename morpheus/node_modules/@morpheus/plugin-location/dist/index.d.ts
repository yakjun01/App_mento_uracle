declare function getCurrentPosition(options?: {
    timeout?: number;
    maximumAge?: number;
}): Promise<{
    status: "SUCCESS";
    coords: {
        latitude: string;
        longitude: string;
    };
} | {
    status: "FAILE";
    error?: string;
    message?: string;
}>;

export { getCurrentPosition };
